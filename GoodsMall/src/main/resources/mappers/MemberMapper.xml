<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="edu.the.joeun.mapper.MemberMapper">

    <resultMap id="resultMember" type="edu.the.joeun.model.Member">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="email" column="email"/>
        <result property="password" column="password"/>
        <result property="role" column="role"/>
        <result property="created_at" column="created_at"/>
        <result property="updated_at" column="updated_at"/>
    </resultMap>

    <select id="getAll" resultMap="resultMember">
        SELECT *
        FROM member
        ORDER BY id DESC
    </select>

    <!-- parameterType: 파라미터로 model 폴더의 Member 클래스를 받음 -->
    <insert id="insertMember" parameterType="edu.the.joeun.model.Member">
        INSERT INTO member (name, email, password, role)
        VALUES (#{name}, #{email}, #{password}, #{role})
    </insert>

</mapper>
